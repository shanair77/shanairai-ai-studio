# ai-agent

The reflect → propose → compile → inspect loop.

```bash
npm install
npm start
```

`compiler.describe()` exposes a JSON-safe catalog; a (stand-in) agent generates a request from
the discovered parameter schema, compiles it, and inspects the structured report.
