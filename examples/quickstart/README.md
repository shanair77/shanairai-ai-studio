# quickstart

The smallest complete AI Studio example — matches the root README's Quick Start.

```bash
npm install
npm start          # defineTemplate → createCompiler → compile
```

`src/index.ts` compiles a request and prints the resulting `BuiltComposition`.
`src/Root.tsx` shows mounting it in a Remotion `<Composition>`.
