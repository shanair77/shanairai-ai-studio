/**
 * transitions/ — Scene-to-scene transition presets.
 *
 * Wraps `@remotion/transitions` (`TransitionSeries`) with on-brand presentations and
 * timings: fade, slide, wipe, zoom, plum-veil. Presets pull duration/easing from
 * `config/Timing.ts` and `config/Animation.ts` so pacing stays consistent everywhere.
 *
 * Distinct from `animations/` (which animates elements within a scene); this module
 * governs the handoff between scenes.
 */

export {};
