/**
 * components/ — Presentational primitives.
 *
 * Low-level, brand-agnostic building blocks that read tokens from the theme:
 * Text, Stack / Row / Grid, Card, Divider, and similar. Everything higher up
 * (titles, layouts, scenes) composes these — they own no content or timing logic.
 *
 * Convention: read color/type/spacing from the active theme; never hardcode a hex,
 * font, or magic number. Re-export each primitive from here as it's added.
 */

export {};
