/**
 * titles/ — Reusable title & text-treatment components.
 *
 * Composed, animated text units: kickers/overlines, animated headlines, lower-thirds,
 * pull quotes, animated word/line reveals. They combine `components/Text` with helpers
 * from `animations/` and read type styles from `config/Typography.ts`.
 *
 * Format-agnostic: a title must read correctly in vertical, horizontal, and square.
 */

export {};
