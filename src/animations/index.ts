/**
 * animations/ — Reusable animation helpers.
 *
 * Frame-driven motion built on `useCurrentFrame()` + `interpolate()`, consuming the
 * presets and easing curves from `config/Animation.ts` and durations from
 * `config/Timing.ts`. Helpers return inline `style` objects (opacity/transform/scale)
 * so they stay editable in Studio.
 *
 * Never use CSS or Tailwind transition/animation classes — they do not render.
 * Examples to add: fadeIn, fadeInUp, scaleIn, stagger, typewriter, kenBurns.
 */

export {};
