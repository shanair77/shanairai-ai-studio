/**
 * effects/ — Visual & pixel effect wrappers.
 *
 * Reusable overlays and filters that add polish: glow, grain/noise, light leaks,
 * vignette, chromatic aberration, blur reveals, gold shimmer. Prefer Remotion's
 * built-in effects and CSS filters; drop to custom `<HtmlInCanvas onPaint>` only
 * when no effect fits.
 *
 * Effects layer above content (`overlay`/`foreground` z-index) and should be subtle.
 */

export {};
