/**
 * icons/ — Icon components.
 *
 * SVG-based, theme-aware icons that accept a `size` (scaled per format) and inherit
 * or take a color token. Keep them as inline React SVG so they animate and recolor
 * cleanly; avoid raster icon files.
 *
 * Used sparingly in video — treat icons as supporting visuals, not dense UI.
 */

export {};
