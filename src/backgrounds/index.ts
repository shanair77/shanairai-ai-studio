/**
 * backgrounds/ — Reusable background components.
 *
 * Full-frame backdrops that sit behind scene content: gradient washes (from
 * `config/Colors.ts` gradients), soft particle/bokeh fields, texture and grain
 * overlays, animated sheen. Rendered with `<AbsoluteFill>` at the `background`
 * z-index layer.
 *
 * Backgrounds are decorative and must never compete with foreground content.
 */

export {};
